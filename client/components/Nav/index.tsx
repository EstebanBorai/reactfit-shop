import * as React from 'react';
import './nav.styl';

const Nav = (props) => (
  <nav className="lobby-nav">
    <header>
      User
    </header>
    <ul>
      <li>
        Menu Item
      </li>
    </ul>
  </nav>
);

export default Nav;
